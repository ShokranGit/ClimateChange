node_modules/
.DS_Store
.env
.env.local
.vercel/
tmp/
# Baked layer files ARE committed — Vercel deploys straight from the repo, so a
# layer that is not in git is not on the site. Anything over ~50 MB should go to
# Supabase Storage or a GitHub release asset instead of the repo.
public/vendor/
