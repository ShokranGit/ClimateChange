// Overwritten at build time on Vercel by scripts/write-env.mjs, which reads
// SUPABASE_URL and SUPABASE_ANON_KEY from the project's environment variables.
// Checked in with empty values so the site works with no backend configured.
window.__SUPABASE_URL__ = '';
window.__SUPABASE_ANON_KEY__ = '';
